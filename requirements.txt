# Runtime dependencies of the submitted Agent: NONE.
#
# agent.py and everything under src/ import only the Python standard
# library — retrieval runs on sqlite3's built-in FTS5 index and stays
# in memory. The Agent makes no model calls and no network calls, so
# there are no credentials to configure to reproduce our results.
#
# pytest is listed because the test suite is part of verifying the
# submission, not because the Agent needs it at runtime.
pytest>=8.0
